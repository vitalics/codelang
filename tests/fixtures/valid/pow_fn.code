fn main() {
  print(pow(2, 0));
  print(pow(2, 10));
  print(pow(3, 5));
  print(pow(10, 6));
}

fn pow(base: int, exp: int): int {
  if exp <= 0 {
    return 1;
  }
  return base * pow(base, exp - 1);
}

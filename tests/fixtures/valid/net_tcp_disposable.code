import "stdlib/network/net";
import "stdlib/network/datagram";
import "stdlib/network/ws";

# net_tcp_disposable.code — verifies Disposable IR declarations on TCP / UDP / WS.
# Does NOT make real network connections — just checks the IR contains the correct
# dispose() wiring by inspecting what gets emitted.
#
# Expected output (1 line):
#   ok

fn main(): void {
    # The Disposable conformance is verified at the IR / compile level.
    # We just confirm the program compiles and runs.
    print("ok");
}
